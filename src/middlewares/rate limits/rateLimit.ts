import {NextFunction} from "express";

export const requestLogger = async (request: Request,
                                    response: Response,
                                    next: NextFunction) => {



}